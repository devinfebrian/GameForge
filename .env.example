# Supabase — Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated emails promoted to the admin role on sign-in.
ADMIN_EMAILS=

# Public origin of this app (auth redirects, sandbox iframe).
NEXT_PUBLIC_APP_ORIGIN=http://localhost:5055

# A second origin that serves generated games as their own document, so previews
# run isolated by origin (storage and ES modules work; the app's cookies do not).
# It MUST differ from NEXT_PUBLIC_APP_ORIGIN. Locally, localhost and 127.0.0.1 are
# distinct origins served by the same dev server. Leave unset to keep using the
# opaque-origin sandbox.
NEXT_PUBLIC_PREVIEW_ORIGIN=http://127.0.0.1:5055

# LLM access for the generation pipeline (/api/generate). Both are optional for
# the rest of the app: only generation fails without them.
#
# These point at an OpenAI-compatible gateway that fronts Claude, NOT at
# api.anthropic.com directly - the gateway returns 404 for /v1/messages. The
# credential is therefore sent as `Authorization: Bearer <token>`, so it is a
# gateway JWT rather than an `sk-ant-` key. See lib/llm/chat-completions.ts.
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=

# Encrypts stored secrets at rest, currently the gateway API key override set from
# /admin. Base64, exactly 32 bytes. Required in production, optional in
# development (a development app with no stored override runs without it).
# Generate one with: openssl rand -base64 32
INTEGRATION_ENCRYPTION_KEY=

# Signs the short-lived token in an isolated preview URL. The preview origin
# carries no session cookie, so this token is the only access check between a
# version id and another user's source. Required in production; optional in
# development, where previews are served outside production only. Any long
# random string.
PREVIEW_TOKEN_SECRET=

# Quota. Tokens per user per UTC day, then runs per user per rolling minute.
# Both have defaults in lib/env/server.ts; set them here to tune without a deploy.
# Admins are exempt from both.
DAILY_TOKEN_BUDGET=250000
RUN_BURST_PER_MINUTE=5

# The sandbox frame is same-host, so there is no PARENT_ORIGIN or SANDBOX_ORIGIN:
# the parent's origin comes from NEXT_PUBLIC_APP_ORIGIN and the asset origin is
# derived from NEXT_PUBLIC_SUPABASE_URL.
